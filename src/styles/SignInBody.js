import styled from "styled-components";
import { ThemeColors } from "../constants/colors";

// Styled component named StyledButton
export const MainContainer = styled.div`
   background-color:${ThemeColors.bodycolor}
 `; 
