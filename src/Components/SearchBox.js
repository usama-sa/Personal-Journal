import { SearchHolder, SearchMain } from "../styles/SearchBox"

const SearchBox = () => {
    return (
        <div>
            <SearchHolder>
                <SearchMain placeholder="Search " id="searchbox">

                </SearchMain></SearchHolder>
        </div>
    )
}

export default SearchBox
